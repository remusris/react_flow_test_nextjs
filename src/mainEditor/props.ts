import { EditorProps } from "@tiptap/pm/view";

const Props: EditorProps = {
  attributes: {
    class:
      "prose-base prose-gray prose-headings:font-bold focus:outline-none prose-h1 prose-h2 prose-h3 prose-p",
  },
};

export default Props;
